# Mobile Apps

Place your mobile app binaries in the respective directories:

- **Android**: Place `.apk` files in `android/` directory
- **iOS**: Place `.app` bundles in `ios/` directory

Example:
- `android/hoi-app-dev.apk`
- `ios/hoi-app-dev.app`

